# Visual Reference Guide

## Node Types Quick Reference

### 1. Trigger Node (No Inputs)
```
┌──────────────────────────────┐
│  💬  When Chat Received     │  ← Rounded (pill shape)
│      Triggers               │
│      [▶] [⚙]                │
└──────────────────────────────●  ← Output only (circle)
```

### 2. Standard Node
```
■──┬─────────────────────────┐
   │  🌐  HTTP Request       │  ← Left border colored
   │      Actions            │
   │      [▶] [⚙]            │
   └─────────────────────────●  ← Output (circle)
     ↑
Input (square)
```

### 3. AI Agent Node
```
         ◆ Chat Model  ◆ Memory  ◆ Tools
         |             |         |
■────┬───┴─────────────┴─────────┴───┐
     │  🤖  AI Agent                 │  ← Larger, multiple top inputs
     │      AI                       │
     │      [▶] [⚙]                  │
     └───────────────────────────────●
       ↑
  Main input                    Output
```

### 4. Chat Model Node (AI Component)
```
■───────────────────────────────┐
│   🔵  GPT-4 Turbo            │  ← Pill shape
│        Chat Models           │
│        [▶] [⚙]               │
└──────────────────────────────●  ← AI output (purple circle)
  ↑
Optional input
```

### 5. Conditional Node (If/Else)
```
                           ● ← True (green)
                          /
■────┬──────────────────┐
     │  🔀  If          │
     │      Flow        │
     │      [▶] [⚙]    │
     └──────────────────┐
       ↑                 \
    Input                 ● ← False (red)
```

### 6. Output Node (No Outputs)
```
■────┬─────────────────────┐
     │  💬  Respond to Chat│  ← No output handles
     │      Output         │
     │      [▶] [⚙]        │
     └─────────────────────┘
       ↑
    Input only
```

## Connection Patterns

### Simple Workflow
```
┌──────────┐     ┌──────────┐     ┌──────────┐
│ Trigger  ●─────■  Action  ●─────■  Output  │
└──────────┘     └──────────┘     └──────────┘
   (pill)       (rectangle)       (rectangle)
```

### AI Agent Workflow
```
┌──────────┐                    ┌──────────┐
│   Chat   ●────────────┐       │  Output  │
│  Model   │            │       └──────────┘
└──────────┘            │              ▲
    (pill)              ▼              │
                     ◆ Chat         ■  │
┌──────────┐          Model         │  │
│ Trigger  ●────────■  AI Agent  ●──┘
└──────────┘          ◆ Memory
   (pill)             │
                      │
┌──────────┐          │
│  Memory  ●──────────┘
└──────────┘
   (pill)
```

### Conditional Flow
```
┌──────────┐     ┌──────────┐     ┌────────────┐
│ Trigger  ●─────■  If/Else ●─────■  Success   │
└──────────┘     └──────────●─┐   └────────────┘
                           │  │
                (green)    │  │   ┌────────────┐
                           │  └───■  Failure   │
                           │      └────────────┘
                      (red)
```

### Multiple Tools to AI Agent
```
┌──────────┐
│Calculator●──────┐
└──────────┘      │
                  ├───◆ Tools
┌──────────┐      │
│Web Search●──────┤
└──────────┘      │   ┌───────────┐
                  ├───◆  AI Agent │
┌──────────┐      │   └───────────┘
│ API Call ●──────┘
└──────────┘
```

## Color Legend

### Node Categories
| Category | Color | Example |
|----------|-------|---------|
| Triggers | 🟢 Green (#10b981) | When Chat Received |
| AI | 🟣 Purple (#8b5cf6) | AI Agent |
| Chat Models | 🟢 Green (#10a37f) | GPT-4 |
| Memory | 🟣 Purple (#8b5cf6) | Simple Memory |
| Tools | 🟢 Green (#10b981) | Calculator |
| Flow | 🟣 Purple (#9C27B0) | If/Else |
| Data | 🔵 Blue (#06b6d4) | Filter |
| Actions | 🟢 Green (#4CAF50) | HTTP Request |
| Output | 🔵 Cyan (#06b6d4) | Respond to Chat |

### Connection Types
| Type | Color | Example |
|------|-------|---------|
| Standard | ⚪ Gray (#999) | Trigger → Action |
| AI Component | 🟣 Purple (#8b5cf6) | Chat Model → AI Agent |
| True Branch | 🟢 Green (#10b981) | If → Success |
| False Branch | 🔴 Red (#ef4444) | If → Failure |

## Handle Shapes Quick Reference

| Shape | Type | Position | Usage |
|-------|------|----------|-------|
| ■ Square | Input | Left | Standard workflow input |
| ● Circle | Output | Right | Standard workflow output |
| ◆ Diamond | AI Input | Top | AI Agent special inputs |
| ● Circle | AI Output | Right | AI component outputs |
| ● Green Circle | Conditional Output | Right | True branch |
| ● Red Circle | Conditional Output | Right | False branch |

## Node State Indicators

### Normal State
```
┌─────────────┐
│  📦  Node   │  ← Normal border
│      Type   │
└─────────────┘
```

### Selected State
```
┌═════════════┐
║  📦  Node   ║  ← Thicker, colored border
║      Type   ║     + glow effect
└═════════════┘
```

### Running State
```
┌─────────────┐
│  📦  Node   │  ← Pulsing yellow border
│  ⏳ Running │     + spinner icon
└─────────────┘
```

### Completed State
```
┌─────────────┐
│  📦  Node   │  ← Green border
│  ✓ Done     │     + checkmark
└─────────────┘
```

### Error State
```
┌─────────────┐
│  📦  Node   │  ← Red border
│  ✗ Error    │     + error icon
└─────────────┘
```

## Workflow Examples

### Example 1: Basic Chat Bot
```
┌──────────────┐
│ When Chat    │
│ Received  ●──┼──────┐
└──────────────┘      │
      (pill)          │
                      ▼
┌──────────────┐    ■ Input
│ GPT-4 Turbo  │      ◆ Chat Model
│           ●──┼────► AI Agent  ●────┐
└──────────────┘      └──────────┘   │
   (pill, AI)                         │
                                      ▼
                               ┌──────────────┐
                               │ Respond to   │
                               │ Chat         │
                               └──────────────┘
```

### Example 2: Conditional Response
```
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ When Chat ●──┼────■ AI Agent  ●──┼────■ If/Else   ●──┼──► Success
└──────────────┘    └──────────────┘    │             │
                           ▲             └─────────────●──┼──► Error
                           │                             │
┌──────────────┐           │                      (green)   (red)
│ GPT-4     ●──┼───────────┘
└──────────────┘
```

### Example 3: Multi-Tool Agent
```
┌──────────────┐
│ Calculator●──┼──┐
└──────────────┘  │
                  ├───◆ Tools
┌──────────────┐  │   ┌──────────────┐    ┌──────────────┐
│ When Chat ●──┼──┼───■ AI Agent  ●──┼────■ Respond      │
└──────────────┘  │   ◆ Chat Model   │    └──────────────┘
                  │   └──────────────┘
┌──────────────┐  │         ▲
│ Web Search●──┼──┘         │
└──────────────┘            │
                   ┌────────┴──────┐
                   │ GPT-4 Turbo●──┘
                   └───────────────┘
```

## Size Reference

| Node Type | Min Width | Min Height | Border Radius |
|-----------|-----------|------------|---------------|
| Standard | 200px | Auto | 8px |
| Trigger | 200px | Auto | 20px (pill) |
| AI Agent | 240px | 100px | 8px |
| AI Component | 160px | Auto | 50px (pill) |
| Output | 200px | Auto | 8px |

## Spacing Guidelines

- **Node padding**: 16px
- **Handle size**: 12px × 12px
- **Handle hover**: 14px × 14px
- **Icon size**: 20px
- **Icon container**: 32px × 32px
- **Border width**: 2px
- **Left border**: 4px (colored)
- **Gap between elements**: 12px

## Interaction States

### Hover
```
Before:  Shadow: 0 2px 8px rgba(0,0,0,0.1)
After:   Shadow: 0 4px 16px rgba(0,0,0,0.15)
         Transform: translateY(-1px)
```

### Selected
```
Border: Primary color
Shadow: 0 0 0 3px rgba(139,92,246,0.2)
```

### Connecting
```
Handle size: 14px × 14px
Handle color: Primary
Connection line: Dashed, animated
```

---

This visual reference provides a complete overview of the n8n-style design system implementation.

