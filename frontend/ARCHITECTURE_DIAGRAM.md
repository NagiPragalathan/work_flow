# Architecture Diagram

## 🏗️ System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         User Interface                          │
│                           (App.jsx)                             │
└──────────────────────┬──────────────────────────────────────────┘
                       │
          ┌────────────┴────────────┐
          │                         │
          ▼                         ▼
┌─────────────────┐       ┌──────────────────┐
│   Components    │       │   Workflow       │
│                 │       │   Management     │
│  • UI           │       │                  │
│  • Workflow     │◄──────┤  • Node Library  │
│  • Execution    │       │  • Property Panel│
└────────┬────────┘       │  • Canvas        │
         │                └──────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────────┐
│                     Core Systems Layer                          │
├─────────────┬─────────────┬─────────────┬─────────────┬────────┤
│  Execution  │  Animation  │ Validation  │  Logging    │ Storage│
│  Engine     │  System     │  System     │  System     │ System │
│             │             │             │             │        │
│ • Executor  │ • States    │ • Rules     │ • Logger    │ • Hook │
│ • Flow      │ • Keyframes │ • Helpers   │ • Levels    │ • API  │
└─────────────┴─────────────┴─────────────┴─────────────┴────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Node Registry                              │
│                                                                 │
│  Triggers → Chat Models → AI → Memory → Tools → Flow → Data    │
└─────────────────────────────────────────────────────────────────┘
```

## 📦 Module Dependency Graph

```
App.jsx
  ├── Components
  │   ├── UI (Toast, ChatBox)
  │   ├── Workflow (WorkflowNode, NodeLibrary, PropertyPanel)
  │   └── Execution (StatusBar, Viewer, ResultModal)
  │
  ├── Hooks
  │   ├── useLocalStorage ──────► localStorage API
  │   ├── useNodeValidation ────► Validation Utils
  │   └── useNodeExecution ─────► Execution Engine
  │
  ├── Execution
  │   ├── executionLogger ──────► Console, Subscribers
  │   └── workflowExecutor ─────► Backend API
  │
  ├── Animations
  │   └── nodeAnimations ───────► CSS Keyframes
  │
  ├── Utils
  │   ├── validation ───────────► Validators
  │   └── formatters ───────────► Formatters
  │
  └── Nodes
      ├── Base (Factories, Properties)
      ├── Triggers
      ├── Chat Models
      ├── AI
      ├── Memory
      ├── Tools
      ├── Flow
      ├── Data
      ├── Actions
      └── Output
```

## 🔄 Execution Flow

```
User Clicks "Execute"
         │
         ▼
┌─────────────────────┐
│  Validation System  │
│  • Check required   │
│  • Check API keys   │
│  • Check connections│
└─────────┬───────────┘
          │ Valid ✓
          ▼
┌─────────────────────┐
│  Workflow Executor  │
│  • Create workflow  │
│  • Send to backend  │
└─────────┬───────────┘
          │
          ▼
┌─────────────────────┐      ┌──────────────┐
│  Execution Logger   │◄─────┤  Backend API │
│  • Log start        │      └──────────────┘
│  • Log each node    │              │
│  • Log completion   │              │
└─────────┬───────────┘              │
          │                          │
          │ For each node:           │
          │   ┌──────────────────────┘
          │   │
          ▼   ▼
┌──────────────────────┐
│  Animation System    │
│  • Set "running"     │
│  • Apply pulse       │
│  • Show spinner      │
└──────────┬───────────┘
           │ Animation delay (200ms)
           ▼
┌──────────────────────┐
│  Animation System    │
│  • Set "completed"   │
│  • Success pulse     │
│  • Show result       │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│  Storage System      │
│  • Save to history   │
│  • Update localStorage│
└──────────────────────┘
```

## 🎯 Node Registration Flow

```
Developer Creates New Node
         │
         ▼
┌──────────────────────────────────┐
│  Define in nodes/{category}/     │
│                                  │
│  createNode({                    │
│    name: "My Node",              │
│    properties: { ... },          │
│    ...                           │
│  })                              │
└──────────────┬───────────────────┘
               │
               ▼
┌──────────────────────────────────┐
│  Register in nodes/index.js      │
│                                  │
│  export nodeTypeDefinitions = {  │
│    ...myNewNode                  │
│  }                               │
└──────────────┬───────────────────┘
               │
               ▼
     ┌─────────┴─────────┐
     │  Automatic Setup  │
     └─────────┬─────────┘
               │
     ┌─────────┼─────────┐
     │         │         │
     ▼         ▼         ▼
┌─────────┐ ┌──────┐ ┌────────┐
│Animation│ │ Log  │ │Storage │
│  Auto   │ │ Auto │ │  Auto  │
└─────────┘ └──────┘ └────────┘
     │         │         │
     └─────────┼─────────┘
               │
               ▼
┌──────────────────────────────────┐
│  Node Ready to Use!              │
│  • Appears in Node Library       │
│  • Has validation                │
│  • Has animations                │
│  • Has logging                   │
│  • Has storage                   │
└──────────────────────────────────┘
```

## 🔌 Component Communication

```
┌────────────────────────────────────────────────────┐
│                    App.jsx                         │
│  (State Management & Orchestration)                │
└───┬────────────┬────────────┬───────────┬─────────┘
    │            │            │           │
    │ Props      │ Props      │ Props     │ Props
    │            │            │           │
    ▼            ▼            ▼           ▼
┌────────┐  ┌────────┐  ┌──────────┐  ┌──────────┐
│Node    │  │Property│  │Execution │  │Status    │
│Library │  │Panel   │  │Viewer    │  │Bar       │
└───┬────┘  └───┬────┘  └────┬─────┘  └────┬─────┘
    │           │            │             │
    │ Callback  │ Callback   │ Callback    │ Callback
    │           │            │             │
    └───────────┴────────────┴─────────────┘
                     │
                     ▼
            ┌────────────────┐
            │  Shared State  │
            │  (via hooks)   │
            └────────────────┘
                     │
         ┌───────────┼───────────┐
         │           │           │
         ▼           ▼           ▼
    ┌────────┐  ┌────────┐  ┌────────┐
    │Storage │  │Execute │  │Logger  │
    └────────┘  └────────┘  └────────┘
```

## 💾 Data Flow

```
┌─────────────┐
│  User Input │
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│ Property Panel  │ ──────► localStorage (immediate)
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│  Node State     │
│  (React State)  │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│  Validation     │
│  (useHook)      │
└──────┬──────────┘
       │ Valid ✓
       ▼
┌─────────────────┐
│  Execution      │
│  (Executor)     │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│  Backend API    │
│  (POST /execute)│
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│  Results        │ ──────► Execution History
└──────┬──────────┘         (localStorage)
       │
       ▼
┌─────────────────┐
│  UI Update      │
│  (Animations)   │
└─────────────────┘
```

## 🎨 Animation State Machine

```
┌─────────┐
│  Idle   │
└────┬────┘
     │ Execute clicked
     ▼
┌─────────┐
│Validating│
└────┬────┘
     │ Valid ✓
     ▼
┌─────────┐
│ Running │◄─────┐
│ (Pulse) │      │ Loop through nodes
└────┬────┘      │
     │           │
     │ Each node │
     ├───────────┘
     │
     │ All done
     ▼
   ┌───┴────┐
   │        │
   ▼        ▼
┌──────┐ ┌──────┐
│Success│ │Error │
│(Green)│ │(Red) │
└───┬──┘ └──┬───┘
    │       │
    └───┬───┘
        │ After 2s
        ▼
    ┌──────┐
    │ Idle │
    └──────┘
```

## 🔍 Hook Dependencies

```
useNodeExecution
      │
      ├──► useState (isExecuting)
      ├──► useState (executionState)
      ├──► useCallback (startExecution)
      └──► useCallback (completeExecution)

useNodeValidation
      │
      ├──► useState (isValid)
      ├──► useState (errors)
      ├──► useCallback (validate)
      ├──► useEffect (auto-validate)
      └──► nodeTypeDefinitions

useLocalStorage
      │
      ├──► useState (storedValue)
      ├──► useCallback (setValue)
      ├──► useCallback (removeValue)
      └──► localStorage API

useNodeProperties
      │
      └──► useLocalStorage
           (wraps with node-specific key)
```

## 🏗️ Build Output

```
dist/
  ├── index.html
  ├── assets/
  │   ├── index-[hash].css    (37.77 KB)
  │   └── index-[hash].js     (458.74 KB)
  │
  └── [Optimized & Tree-shaken]
       │
       ├── Core App Bundle
       ├── Components (code-split)
       ├── Hooks (tree-shaken)
       ├── Utils (tree-shaken)
       └── Node Definitions
```

## 📚 Import Chain Example

```javascript
App.jsx
  │
  ├─► import { WorkflowNode } from './components'
  │        │
  │        └─► components/index.js
  │                │
  │                └─► export * from './workflow'
  │                        │
  │                        └─► workflow/index.js
  │                                │
  │                                └─► export WorkflowNode
  │
  ├─► import { useNodeValidation } from './hooks'
  │        │
  │        └─► hooks/index.js
  │                │
  │                └─► export * from './useNodeValidation'
  │
  └─► import { executionLogger } from './execution'
           │
           └─► execution/index.js
                   │
                   └─► export * from './executionLogger'
```

---

## 📖 Key Takeaways

1. **Separation of Concerns**: Each module has ONE job
2. **Dependency Injection**: Systems are loosely coupled
3. **Reusability**: Write once, use everywhere
4. **Scalability**: Easy to add new features
5. **Maintainability**: Clear structure, easy to navigate
6. **Performance**: Tree-shaking removes unused code
7. **Type Safety**: Consistent interfaces throughout

---

**This architecture supports:**
- ✅ Adding unlimited nodes (just register)
- ✅ Custom animations (extend system)
- ✅ Custom validations (add rules)
- ✅ Custom formatters (add functions)
- ✅ Custom hooks (add to hooks/)
- ✅ New components (add to components/)
- ✅ Future features (modular design)

